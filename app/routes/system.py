"""System routes — health, reload, shutdown, startup, watcher, memory, stats.

Currently defined in rag_api.py. Will be migrated here incrementally.
See app/services/system_service.py and app/services/memory_service.py
for extracted logic.
"""

# Route registration happens in rag_api.py for now.
